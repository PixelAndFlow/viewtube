services:
  - type: web
    name: viewtube-backend
    runtime: node
    rootDir: server
    buildCommand: cd ../client && npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DB_PATH
        value: /tmp/viewtube.db
