services:
  - type: web
    name: viewtube
    runtime: node
    plan: free
    branch: mofazzal-setup
    buildCommand: npm run render-build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 22.12.0
